const codeLanguage = 'javascript'
const code = `export default {
  strategy: 'predictive',
  engine: {
    cpus: 12,
    backups: ['./storage/cache.wtf'],
  },
}`

export function Hero() {
  return (
    <div className="pt-48">
    </div>
  )
}
